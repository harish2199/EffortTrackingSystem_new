@model CommonDataAccess.Models.Leave

<head>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>

<table class="table">
    <thead>
        <tr>
            <th>User</th>
            <th>Date</th>
            <th>Reason</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @if (ViewBag.PendingLeaves != null)
        {
            foreach (var leave in ViewBag.PendingLeaves)
            {
                <tr>
                    <td>@leave.User.UserName</td>
                    <td>@leave.Date</td>
                    <td>@leave.Reason</td>
                    <td>
                        <form action="/admin/ApproveLeave?leaveid=@leave.LeaveId" method="post">
                            <button type="submit" name="action" value="Approved" class="btn btn-success">Approve</button>
                            <button type="submit" name="action" value="Rejected" class="btn btn-danger">Reject</button>
                        </form>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>


<script src="~/bundles/jquery"></script>
<script src="~/bundles/bootstrap"></script>
